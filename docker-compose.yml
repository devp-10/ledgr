services:
  # ── Ollama (local LLM) ────────────────────────────────────────────────────
  ollama:
    image: ollama/ollama
    volumes:
      - ollama_data:/root/.ollama   # model weights persist across restarts
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  # Pull model on first run (cached in volume; subsequent starts are instant)
  ollama-init:
    image: ollama/ollama
    depends_on:
      ollama:
        condition: service_healthy
    environment:
      - OLLAMA_HOST=http://ollama:11434
      - OLLAMA_MODEL=${OLLAMA_MODEL:-mistral}
    entrypoint: ["sh", "-c", "ollama pull $${OLLAMA_MODEL}"]
    restart: "no"

  # ── FastAPI backend ───────────────────────────────────────────────────────
  backend:
    build: ./app/backend
    ports:
      - "8000:8000"                # API docs at http://localhost:8000/docs
    environment:
      - OLLAMA_URL=http://ollama:11434
      - OLLAMA_MODEL=${OLLAMA_MODEL:-mistral}
      - DB_PATH=/data/expenses.db
    volumes:
      - ./data:/data               # SQLite database lives at ./data/expenses.db on the host
    depends_on:
      ollama:
        condition: service_healthy

  # ── React frontend (built + served by nginx) ──────────────────────────────
  frontend:
    build: ./app/frontend
    ports:
      - "3000:80"                  # open http://localhost:3000
    depends_on:
      - backend

volumes:
  ollama_data:   # model weights (downloaded once, persisted across restarts)
